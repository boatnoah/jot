# Jot — Domain Context

## Glossary

### Session
A single capture lifecycle from the moment the user presses Start to the moment they press Stop. A session produces exactly one transcript and one notes file. Only one session may be active or processing at a time.

### Recording Folder
The output directory for a session. Created at Start with the datestamp-only name `YYYY-MM-DD HH-mm`. Contains all session artifacts from the beginning: chunk files, `metadata.json`, and eventually `transcript.md` and `notes.md`. Renamed to `YYYY-MM-DD HH-mm <Generated Title>` after Codex returns a title. If Codex fails, the folder retains the datestamp-only name. No temp directory is used — the folder is the single stable location for crash recovery and retry.

### Chunk
A segment of raw audio captured during a session. Chunks are written in 30-second increments, matching Whisper's native processing window. A chunk may be shorter than 30 seconds if the user pauses or stops mid-cycle. Each chunk index always produces two files: one mic file and one system audio file, even if one stream is silent. Each chunk carries an `elapsedOffset` — the total non-paused session time that preceded it — so Whisper's chunk-relative timestamps can be converted to session-elapsed timestamps. Transcription of a chunk pair begins immediately when the chunk is closed, running in the background while the next chunk is being recorded.

### Streaming Transcription
The behavior where chunk transcription runs concurrently with recording. When Stop is pressed, all but the final chunk are typically already transcribed. The post-Stop "Transcribing" state is therefore just waiting for the last in-flight chunk to finish before merging.

### Elapsed Time
Session time with paused intervals excluded. All transcript timestamps are elapsed time, not wall-clock time. Pausing does not advance elapsed time.

### Transcript
The durable checkpoint for a session. A Markdown file where each line is a timestamped, speaker-tagged segment. Timestamps are session-elapsed. Speaker tags are `User` (microphone) or `Others` (system audio). Segments are sorted by elapsed timestamp; ties within 500 ms put `User` before `Others`.

### Notes
The Markdown summary generated by the notes agent (Codex in v1) from the full transcript. Structured with Summary, Decisions, Action Items, Open Questions, Follow-ups, and Source sections. Timestamps in Notes cite elapsed time from the transcript.

### Context Cap
A pre-flight check run before sending the transcript to the Notes Agent. Estimated by character count (1 token ≈ 4 chars) against a ceiling of ~100k tokens (~8 hours of dense transcript). If exceeded, the transcript is saved and notes generation fails with a clear message — no recursive summarization. There is no recording duration cap; the user decides when to stop.

### Notes Agent
The external CLI invoked non-interactively to generate Notes from a Transcript. The adapter interface is `NotesAgent`. The only v1 implementation is `CodexAgent`.

### System Audio Permission
System audio capture uses ScreenCaptureKit, which requires the Screen Recording permission in macOS System Settings. The permission prompt says "Screen Recording" — Jot explains this with copy: "Jot needs Screen Recording permission to capture system audio. Jot does not record your screen." No fallback to mic-only in v1.

### Transcriber
The internal component that converts a raw audio Chunk into a list of `TranscriptSegment` values. The only v1 implementation is `WhisperCppTranscriber`.
